//! Request to /organiser endpoint

use crate::RequestError;
// use crate::HTTP_PREFIX;
use totsugeki::Organiser;
//use totsugeki::OrganiserPOST;

/// Fetch organisers
pub fn fetch() -> Result<Vec<Organiser>, RequestError> {
    todo!()
}
