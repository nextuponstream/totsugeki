[workspace]
resolver = "2"
members = [
    "totsugeki-core",
    "totsugeki-display",
    "tournament-organiser-api",
    "ui",
]

# otherwise cannot build because those crates believes they are in a workspace
exclude = [
    "examples/totsugeki-discord-bot",
]
