//! Partition loser brackets

use totsugeki::{matches::Match, player::Participants};

use crate::DisplayableMatch;
pub(crate) fn loser_bracket(
    matches: Vec<Match>,
    participants: &Participants,
) -> Vec<Vec<DisplayableMatch>> {
    vec![]
}
